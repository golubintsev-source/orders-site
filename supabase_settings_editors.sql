-- Редакторы окон (учитываются в системе): одно текстовое поле на человека.
-- Можно указать только имя или имя с инициалами — без жёсткого формата.
-- Значение — JSON-массив строк, например: ["Иван","С. И.","Пётр"]
INSERT INTO app_settings (key, value) VALUES ('editors', '[]')
  ON CONFLICT (key) DO NOTHING;
