-- =============================================================================
-- Снять UNIQUE(comment) после восстановления dedup-строк
--
-- Индекс calculations_comment_uq мешает повторной вставке и был причиной
-- физического DELETE дубликатов (в т.ч. ложных совпадений по comment).
--
-- Выполнить в Supabase SQL Editor (postgres). Можно повторно.
-- =============================================================================

DROP INDEX IF EXISTS calculations_comment_uq;

-- Опционально: более безопасная уникальность только для авто-строк заказа
-- (comment + amount + from + to + created_at в пределах минуты) — не включено,
-- пока в calculations нет order_id / delta_key в БД.
