-- =============================================================================
-- Снять широкий UNIQUE(comment) — причина «Ошибка при добавлении» на странице
-- «Расчеты» при повторном ручном комментарии (например «Протект»).
--
-- Индекс calculations_comment_uq мешает повторной вставке обычных строк.
-- После снятия можно выполнить обновлённый
-- supabase_calculations_dedup_unique_comment.sql — он поставит UNIQUE только
-- на авто-комментарии [AUTO_ORDER_DELTA] / [AUTO_EXCESS_DELTA].
--
-- Выполнить в Supabase SQL Editor (postgres). Можно повторно.
-- =============================================================================

DROP INDEX IF EXISTS calculations_comment_uq;
